media:
  input: images
  output: /images

content:
  - name: home
    label: Home Page
    type: file
    path: content/home.yml
    fields:
      - name: title
        label: Title
        type: string

      - name: subtitle
        label: Subtitle
        type: string

      - name: description
        label: Description
        type: text
